# 引き継ぎメモ — CrazyBowling

新しいスレッドの冒頭に貼るための要約。
**詳しい数値と経緯は `仕様.md` にある。まずそちらを読むこと。**

最終更新：2026-09-20 ／ 最新コミット `56ff2b9`（未 push）

---

## これは何か

Unity 6000.3.24f1 / URP の3Dボウリングゲーム。
**変わったレーン10種を1フレームずつ回り、合計点を競う。**
公開先は Web（PLiCy）と Windows。作っているのはUnity初心者の個人。

---

## 今どこまで出来ているか

**遊べる状態になっている。** 10レーン回って点が出て、リザルトまで通る。

| 段階 | 状態 |
|---|---|
| 1. レーンと投球 | 完了 |
| 2. ピンと倒れ判定（連鎖爆発の調整含む） | 完了 |
| 3. スコアとゲーム進行・UI・フォント | 完了 |
| 4. ギミックレーン前半（2〜6本目） | **完了。6本目まで完成** |
| 5. ギミックレーン後半（7〜10本目） | **進行中。7本目 ROTATING TUBE 完成** |
| 6. 仕上げ（タイトル・音・ハイスコア・Webビルド） | 未着手 |

出来ているレーン：STRAIGHT ／ TILTED FLOOR ／ ROLLING FLOOR ／ MOVING WALL ／ SPINNING DISC ／
MOGUL ／ **ROTATING TUBE**。
次は8本目（低重力）。

EditModeテスト **256件すべて成功**。Console はエラー0・警告0。

---

## 先に読む（誤解しやすい点・踏みやすい罠）

### ★★レーンの並びは「読める → 読めない」の軸

**7本目以降、再現性は合格条件ではない。** ばらつくのが仕様。

| | 1〜6本目 | 7〜10本目 |
|---|---|---|
| 同じ投球 | 同じ結果になる（**不一致は不具合**） | ばらついてよい（**仕様**） |
| 狙い | 覚えれば取れる | 傾向は残るが外れる |

ただし**「壊れている」と「荒れている」は別**。7本目以降でも、
**ボールが後ろへ吹き飛ぶ／仕掛けから出てこない／決着しない**は引き続き直す。
ピンに向かって飛んでいく限りは可。

逆に**1〜6本目を直すときは再現性が要る**。詳細は `仕様.md` のコンセプトの節。

### ★曲面を「板を並べて」作ってはいけない

床を複数の面で作ると、ボールが**次の面の上面の後ろ角**を拾って上へ弾かれる。
**継ぎ目の段差が 0.00mm でも起きる。** 角が接触判定の距離
（両者の contactOffset の和＝0.02m）の内側に入ると、面ではなく角が拾われるため。
6本目でこれに 30〜56cm 弾かれ、原因の特定まで何度も設計をやり直した。

**回避策は「板をやめて球で作る」。**
`SphereCollider` には辺も角も分割も無いので、この問題が原理的に起きない。
6本目のコブはこの方法で作ってある。

**7本目で分かった追加。**

- **`CapsuleCollider`（円柱＋両端の半球）も角が無い。** 球と違い「長い棒」が作れるので、
  レールや案内はこれで作る。7本目の羽根24本と入口の案内4本はすべてカプセル
- **角は触れていなくても蹴る。** 実測で、角から **4.6mm 離れた位置**で
  +0.713m/s 弾かれた。接触予測の範囲（2cm）に入った角に対し、
  前進速度を打ち消す向きの力が出るため
- **角を隠す場所があるなら埋める。** 7本目の羽根は、先端を筒の壁の厚みの中
  （軸半径0.645m・壁は0.60から外へ0.10）に入れてある。
  先端をボールの通り道に出したときは**全投が真後ろへ弾き返された**

効かなかった対処（すべて検証済み。もう試さなくてよい）：
- `maxDepenetrationVelocity` を下げる（数値が一桁まで変わらない）
- 面を前へ重ねて次の面の角を埋める（角は共有点にあるので覆えない）
- 面を細かく分ける（角が増えて悪化する）

**ただし球で作れるのは「コブ」のような凸の塊だけ。**
筒の内側や、長く連続する曲面は球では作れない。**ここは未解決。**
7本目の筒状回転で最初に考えるべき論点になる。

考えられる筋（どれも未検証）：
- 球や `CapsuleCollider` を並べて曲面を近似する（角が無い形だけで構成する）
- 床は分割せず、レーン全体を1つの剛体として傾けたり回したりする
- 当たり判定は単純な形のままにして、見た目だけ曲面にする
  （5本目の円盤で採った手。当たり判定を持たせず力だけを加える）

**7本目の筒状回転と10本目のジェットコースターは曲面を作ることになるので、
着手前に必ず `仕様.md` の「保留にしてあること 1」と
「コブと踏切のあるレーン」を読むこと。**

### そのほか、よく誤解される点

### 「操作の作り直し（3段階方式・スマホ対応）」は**不採用として決着済み**
立ち位置 → 角度 → カーブ を3回タップで決める案は**採らなかった**。
手数が多くスマホで投げるまでが長いため。
代わりに**引きベクトル1動作方式**が入っていて、スマホ対応もこれで済んでいる。
未実装ではない。

### 「入射速度 7.44」は**古い値**
操作の作り直しで離し方が変わったため、回帰チェックの基準は **8.36 m/s** に差し替え済み。
古い会話に 7.44 が出てきたら、それは作り直す前の値。

### 「オイル区画」は**実装済み**
レーンの手前10mを低摩擦にして、カーブが手前ではなく**奥で曲がる**ようにしてある。
コミット `2ed88e6`。

---

## 決まっていること

- **得点はレーン完結型。** ストライク30点、スペア20点、それ以外は本数。
  ボーナス投は無い。倍率なしの満点は300点
- **操作は引きベクトル1動作。** 押した位置で立ち位置、引きベクトルで強さと角度、
  カーブは画面下のスライダー
- **角度の上限は2度で固定。** レーンごとの上書きは、足りないレーンが実際に出てから
- **文言の方針**：固定文言とレーン名は英語（STRIKE / TOTAL / MOVING WALL）。
  レーンの一言説明は日本語だが、「アメリカ製の商品に付いてきた微妙な説明書」の文体。
  すべて `Assets/Scripts/UI/UIText.cs` にまとめてあり、方針もそのファイルの冒頭にある
- **フォントは Static で焼き込み済み。** 焼いた文字の一覧は `Assets/Fonts/焼き込んだ文字.md`。
  新しい文字を足したら焼き直しが要る
- **動くものの作り方**：`LaneBehaviour.OnLaneFixedUpdate` から `MovingObstacle` を動かす。
  kinematic Rigidbody ＋ `MovePosition`。Transform を直接動かすとすり抜ける

---

## 作業の進め方（守ること）

- **応答は日本語。C#のコメントも日本語**
- **Unity CLI を使う**（PowerShellで `unity command ...`）。MCP for Unity は予備
  - `unity command eval_file <パス>` は**位置引数**。`--json` では渡らない
  - eval のコードに `using` 文は書けない。`UnityEditor.AssetDatabase` のように全部書く
  - **メインスレッドの上限は5000ms。** 重い計測はこれを超えるとエラーがConsoleに出る
- **シーンに変更を加えたらその場で保存する。** Play前・recompile前・テスト前は必ず
- **`.unity` / `.meta` / `ProjectSettings/` / `manifest.json` は手で書き換えない**
- **自動実行されるコードを作らない**（`[InitializeOnLoad]` など禁止）
- **段階を分けて実装し、各段階ごとに報告して指示を待つ**
- **実装前に、何をどう変えるつもりかを日本語で説明する**
- **変更後は必ず Console を確認して報告する**
- **コミットはしてよいが、push はユーザーの確認を取ってから**
- ユーザーはUnity初心者。手作業が要るときはメニューの場所とクリック順まで書く

---

## ハマりどころ

- **プレハブを書き換えても、Playモードに入り直すまで反映されないことがある。**
  「付けたはずの部品が無い」と出たら、Playを抜けて入り直す
- **`run_tests` は30秒でパイプラインが切れる。** 切れてもテスト自体は走りきっている。
  「Unityが固まった」と早合点しないこと（過去に何度もやった）
- **テストを流すとシーンを開き直すので、未保存だと保存ダイアログが出る。** 先に保存する
- **Enter Play Mode Settings で Reload Scene を切ってある。** Domain Reload は切っていない
- `MaterialPropertyBlock.SetColor` は sRGB→リニア変換をしない
- **自分のGameObjectを自分で SetActive(false) してはいけない。** Updateが止まって復帰できなくなる
- **床を複数の面（BoxCollider や MeshCollider）で作ると、凸の区間でボールが弾かれる。**
  隣の面の「上面の後ろ角」が接触判定の距離（両者の contactOffset の和＝0.02m）の
  内側に入り、面ではなく角が拾われる。継ぎ目の段差が 0.00mm でも起きる。
  6本目で 30〜56cm 弾かれた。`ROLLING FLOOR` の 1.8cm も同じ原因。
  **面を重ねても、`maxDepenetrationVelocity` を下げても直らない（検証済み）。**
  詳しくは `仕様.md` の「保留にしてあること 1」。
  **7本目の筒状回転と10本目のジェットコースターの前に必ず読むこと**
- **回る円盤の計算式は床の摩擦を見込んでいない。**
  加えた力の積算（調整用ログ）の**7割ほどしかボールに残らない**。
  ログの数字をそのまま到達位置に換算しないこと
- **無地の円盤は、回しても回っているように見えない。** 回転軸まわりに対称なため。
  羽根・ずらした印など、向きの分かる模様が要る

- **物理を計測するときの作法（守らないと結論を間違える）**
  - **構え中のボールはマウスのx位置に追従して横に滑る。**
    計測の前に `SetInputBlocked(true)` と `ReturnToSpawn()` の**両方**が要る。
    片方だけだと、右端に寄った状態から投げてガターに落ちる
  - **`ReturnToSpawn()` の直後に `transform.position` を読んでも当てにならない。**
    kinematic から戻る時点の Rigidbody の内部位置は前のまま。
    投げる前の**絶対座標**もログに出すこと。出さないと、ガターに落ちた投球を
    「物理が変わった」と誤読する（実際に一度誤読した）
  - **立ち位置は `_sideOffset` を直接指定する必要がある。**
    投げたあとに `rb.position` へ書いても PhysX には届かない。
    リフレクションで `_sideOffset` を入れ、`ApplySpawnPosition()` を呼ぶこと
  - **速い掃引でピンだけ戻すときは `ThrowSequencer.StopLane()` を呼ぶこと。**
    呼ばないと投球係が投球数を数え続け、**2投ごとにレーンが進む**。
    気づかないまま別のレーンを測ることになる（6本目の掃引で実際にやった）。
    見抜く手がかりは**速度 8.36 m/s**。平らなレーンの回帰基準値なので、
    ギミックのあるレーンでこの値が出たら別のレーンを測っている
  - **Play 中にプレハブを書き換えると反映されないことがある。**
    書き換えたら Play を入り直すこと。
    「直したはずなのに結果が変わらない」はこれを疑う
  - **「初接触z」は低い弾道では当てにならない。**
    ボールが隙間の上を通過している最中に高さ 0.17m を下回ると、そこで記録してしまう。
    隙間の終わりより手前の値が出たら疑うこと
  - **立ち位置の指定が物理に届くまで1フレーム要る。**
    置く呼び出しと投げる呼び出しを**分ける**こと。
    同じ eval の中で続けて投げると指定が効かず、中央から投げたのと同じ結果になる
    （今回、0.30 と 0.45 を指定したのに3本とも中央と同じ値が出た）

- **`BallController.FixedUpdate` は先頭で `_sideSpin` がほぼ0なら即 return している。**
  カーブ0のストレート投球では横回転が0なので、レーン側の力（回る円盤など）を
  この return より後ろに書くと、**ストレートでだけ効かない**。
  「たまに効かない」ように見えて原因が分かりにくい。
  円盤の処理は `_sideSpin` の判定より前に置くこと（`Assets/Scripts/Ball/BallController.cs:416`）

---

## 次にやること（優先順）

1. **SPINNING DISC と MOGUL を遊んで確かめる。**
   円盤は回っているのが見て分かるか。MOGUL はコブで散らされる感じが読めるか。
   数値は実測で合わせてあるが、**遊んだ感じで決め直してよい**
2. **8本目（低重力）。** 作る前に必ず次の3つを読むこと。
   - `仕様.md`「コンセプト」の設計方針（読める→読めない）。**8本目も「読めない側」**
   - `仕様.md`「筒の中を駆け上がるレーン（7本目）」の採らなかった案
   - `仕様.md`「保留にしてあること 1」（面の角を踏む問題）と
     「コブと踏切のあるレーン」（球・カプセルで作る回避策）
   - 保留事項に「レーン全体をU字にする案」がある。**低重力と相性が良い**
3. 9・10本目を順に。**10本目のジェットコースターでは、
   7本目で未解決のまま残した「入口の蹴り」に再び当たる可能性がある**
4. 段階6の仕上げ

> **回る円盤では爆発の判定基準がずれる件は、まだ対応していない。**
> 7本目は筒の当たり判定を回さず（回すのは見た目と羽根だけ）、
> ピンも動かさないので、爆発の判定は変更不要のまま済んでいる。
> 床そのものを回すレーンを作るときに対応が要る。

> **回る円盤では爆発の判定基準がずれる件は、まだ対応していない。**
> 円盤は床を回さず力だけを加えるので、今のところ問題は出ていない。
> 筒状回転（7本目）で床そのものが回るときに対応が要る。

レーンが4本では「10種を回る」という面白さが判断できないので、
仕上げより先にレーンを増やす方針。

---

## 保留にしてあること（今は触らない）

- 起伏レーンでボールが約1.8cm浮く（許容と判断。床の分割を細かくすると悪化する）
- 動く人型が当たり判定より背が高い。32投調べてピンが重なった回数は0だったので何もしない。
  気になったら、頭のぶんの箱を足して `Physics.IgnoreCollision` でボールだけ除外する
- 予測線がピン位置で1割ほど大きく出る（実害なし）
- リザルトの内訳が桁で揃わない（プロポーショナルフォントのため）
- スマホ実機での確認

