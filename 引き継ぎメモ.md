# 引き継ぎメモ — CrazyBowling

新しいスレッドの冒頭に貼るための要約。
**詳しい数値と経緯は `仕様.md` にある。まずそちらを読むこと。**

最終更新：2026-09-26 ／ 最新コミット：段階6 2本目の見た目（このメモを含むコミット）
**8〜10本目の Bloom の作り直しまで push 済み。2本目の見た目はコミットのみ（push は確認待ち）。**

---

## これは何か

Unity 6000.3.24f1 / URP の3Dボウリングゲーム。
**変わったレーン10種を1フレームずつ回り、合計点を競う。**
公開先は Web（PLiCy）と Windows。作っているのはUnity初心者の個人。

---

## 今どこまで出来ているか

**遊べる状態になっている。** 10レーン回って点が出て、リザルトまで通る。

| 段階 | 状態 |
|---|---|
| 1. レーンと投球 | 完了 |
| 2. ピンと倒れ判定（連鎖爆発の調整含む） | 完了 |
| 3. スコアとゲーム進行・UI・フォント | 完了 |
| 4. ギミックレーン前半（2〜6本目） | **完了。6本目まで完成** |
| 5. ギミックレーン後半（7〜10本目） | **完了。10本目まで完成** |
| 6. 仕上げ（タイトル・音・ハイスコア・Webビルド） | **進行中。ミラーボールの全レーン化・1本目・2本目・8〜10本目の Bloom の作り直し済み** |

出来ているレーン：STRAIGHT ／ TILTED FLOOR ／ ROLLING FLOOR ／ MOVING WALL ／ SPINNING DISC ／
MOGUL ／ **ROTATING TUBE** ／ **LOW GRAVITY** ／ **TEMPLE OF STRIKE** ／ **BALL COLLIDER**。
10本目は粒子加速器の研究所。説明文は「本装置はボウリングの球を加速するために建設されました。ほかの用途はございません。」

EditModeテスト **256件すべて成功**。Console はエラー0・警告0。

---

## 先に読む（誤解しやすい点・踏みやすい罠）

### ★★レーンの並びは「読める → 読めない」の軸

**7本目以降、再現性は合格条件ではない。** ばらつくのが仕様。

| | 1〜6本目 | 7〜10本目 |
|---|---|---|
| 同じ投球 | 同じ結果になる（**不一致は不具合**） | ばらついてよい（**仕様**） |
| 狙い | 覚えれば取れる | 傾向は残るが外れる |

ただし**「壊れている」と「荒れている」は別**。7本目以降でも、
**ボールが後ろへ吹き飛ぶ／仕掛けから出てこない／決着しない**は引き続き直す。
ピンに向かって飛んでいく限りは可。

逆に**1〜6本目を直すときは再現性が要る**。詳細は `仕様.md` のコンセプトの節。

### ★曲面を「板を並べて」作ってはいけない

床を複数の面で作ると、ボールが**次の面の上面の後ろ角**を拾って上へ弾かれる。
**継ぎ目の段差が 0.00mm でも起きる。** 角が接触判定の距離
（両者の contactOffset の和＝0.02m）の内側に入ると、面ではなく角が拾われるため。
6本目でこれに 30〜56cm 弾かれ、原因の特定まで何度も設計をやり直した。

**回避策は「板をやめて球で作る」。**
`SphereCollider` には辺も角も分割も無いので、この問題が原理的に起きない。
6本目のコブはこの方法で作ってある。

**7本目で分かった追加。**

- **`CapsuleCollider`（円柱＋両端の半球）も角が無い。** 球と違い「長い棒」が作れるので、
  レールや案内はこれで作る。7本目の羽根24本と入口の案内4本はすべてカプセル
- **角は触れていなくても蹴る。** 実測で、角から **4.6mm 離れた位置**で
  +0.713m/s 弾かれた。接触予測の範囲（2cm）に入った角に対し、
  前進速度を打ち消す向きの力が出るため
- **角を隠す場所があるなら埋める。** 7本目の羽根は、先端を筒の壁の厚みの中
  （軸半径0.645m・壁は0.60から外へ0.10）に入れてある。
  先端をボールの通り道に出したときは**全投が真後ろへ弾き返された**

効かなかった対処（すべて検証済み。もう試さなくてよい）：
- `maxDepenetrationVelocity` を下げる（数値が一桁まで変わらない）
- 面を前へ重ねて次の面の角を埋める（角は共有点にあるので覆えない）
- 面を細かく分ける（角が増えて悪化する）

**ただし球で作れるのは「コブ」のような凸の塊だけ。**
筒の内側や、長く連続する曲面は球では作れない。**ここは未解決。**
7本目の筒状回転で最初に考えるべき論点になる。

考えられる筋（どれも未検証）：
- 球や `CapsuleCollider` を並べて曲面を近似する（角が無い形だけで構成する）
- 床は分割せず、レーン全体を1つの剛体として傾けたり回したりする
- 当たり判定は単純な形のままにして、見た目だけ曲面にする
  （5本目の円盤で採った手。当たり判定を持たせず力だけを加える）

**7本目の筒状回転と10本目のジェットコースターは曲面を作ることになるので、
着手前に必ず `仕様.md` の「保留にしてあること 1」と
「コブと踏切のあるレーン」を読むこと。**

### そのほか、よく誤解される点

### 「操作の作り直し（3段階方式・スマホ対応）」は**不採用として決着済み**
立ち位置 → 角度 → カーブ を3回タップで決める案は**採らなかった**。
手数が多くスマホで投げるまでが長いため。
代わりに**引きベクトル1動作方式**が入っていて、スマホ対応もこれで済んでいる。
未実装ではない。

### 「入射速度 7.44」は**古い値**
操作の作り直しで離し方が変わったため、回帰チェックの基準は **8.36 m/s** に差し替え済み。
古い会話に 7.44 が出てきたら、それは作り直す前の値。

### 「オイル区画」は**実装済み**
レーンの手前10mを低摩擦にして、カーブが手前ではなく**奥で曲がる**ようにしてある。
コミット `2ed88e6`。

---

## 決まっていること

- **得点はレーン完結型。** ストライク30点、スペア20点、それ以外は本数。
  ボーナス投は無い。倍率なしの満点は300点
- **操作は引きベクトル1動作。** 押した位置で立ち位置、引きベクトルで強さと角度、
  カーブは画面下のスライダー
- **角度の上限は2度で固定。** レーンごとの上書きは、足りないレーンが実際に出てから
- **文言の方針**：固定文言とレーン名は英語（STRIKE / TOTAL / MOVING WALL）。
  レーンの一言説明は日本語だが、「アメリカ製の商品に付いてきた微妙な説明書」の文体。
  すべて `Assets/Scripts/UI/UIText.cs` にまとめてあり、方針もそのファイルの冒頭にある
- **フォントは Static で焼き込み済み。** 焼いた文字の一覧は `Assets/Fonts/焼き込んだ文字.md`。
  新しい文字を足したら焼き直しが要る
- **動くものの作り方**：`LaneBehaviour.OnLaneFixedUpdate` から `MovingObstacle` を動かす。
  kinematic Rigidbody ＋ `MovePosition`。Transform を直接動かすとすり抜ける

---

## 作業の進め方（守ること）

- **応答は日本語。C#のコメントも日本語**
- **Unity CLI を使う**（PowerShellで `unity command ...`）。MCP for Unity は予備
  - `unity command eval_file <パス>` は**位置引数**。`--json` では渡らない
  - eval のコードに `using` 文は書けない。`UnityEditor.AssetDatabase` のように全部書く
  - **メインスレッドの上限は5000ms。** 重い計測はこれを超えるとエラーがConsoleに出る
- **シーンに変更を加えたらその場で保存する。** Play前・recompile前・テスト前は必ず
- **`.unity` / `.meta` / `ProjectSettings/` / `manifest.json` は手で書き換えない**
- **自動実行されるコードを作らない**（`[InitializeOnLoad]` など禁止）
- **段階を分けて実装し、各段階ごとに報告して指示を待つ**
- **実装前に、何をどう変えるつもりかを日本語で説明する**
- **変更後は必ず Console を確認して報告する**
- **コミットはしてよいが、push はユーザーの確認を取ってから**
- ユーザーはUnity初心者。手作業が要るときはメニューの場所とクリック順まで書く

---

## ハマりどころ

- **プレハブを書き換えても、Playモードに入り直すまで反映されないことがある。**
  「付けたはずの部品が無い」と出たら、Playを抜けて入り直す
- **`run_tests` は30秒でパイプラインが切れる。** 切れてもテスト自体は走りきっている。
  「Unityが固まった」と早合点しないこと（過去に何度もやった）
- **テストを流すとシーンを開き直すので、未保存だと保存ダイアログが出る。** 先に保存する
- **Enter Play Mode Settings で Reload Scene を切ってある。** Domain Reload は切っていない
- `MaterialPropertyBlock.SetColor` は sRGB→リニア変換をしない
- **自分のGameObjectを自分で SetActive(false) してはいけない。** Updateが止まって復帰できなくなる
- **床を複数の面（BoxCollider や MeshCollider）で作ると、凸の区間でボールが弾かれる。**
  隣の面の「上面の後ろ角」が接触判定の距離（両者の contactOffset の和＝0.02m）の
  内側に入り、面ではなく角が拾われる。継ぎ目の段差が 0.00mm でも起きる。
  6本目で 30〜56cm 弾かれた。`ROLLING FLOOR` の 1.8cm も同じ原因。
  **面を重ねても、`maxDepenetrationVelocity` を下げても直らない（検証済み）。**
  詳しくは `仕様.md` の「保留にしてあること 1」。
  **7本目の筒状回転と10本目のジェットコースターの前に必ず読むこと**
- **回る円盤の計算式は床の摩擦を見込んでいない。**
  加えた力の積算（調整用ログ）の**7割ほどしかボールに残らない**。
  ログの数字をそのまま到達位置に換算しないこと
- **無地の円盤は、回しても回っているように見えない。** 回転軸まわりに対称なため。
  羽根・ずらした印など、向きの分かる模様が要る

- **物理を計測するときの作法（守らないと結論を間違える）**
  - **構え中のボールはマウスのx位置に追従して横に滑る。**
    計測の前に `SetInputBlocked(true)` と `ReturnToSpawn()` の**両方**が要る。
    片方だけだと、右端に寄った状態から投げてガターに落ちる
  - **`ReturnToSpawn()` の直後に `transform.position` を読んでも当てにならない。**
    kinematic から戻る時点の Rigidbody の内部位置は前のまま。
    投げる前の**絶対座標**もログに出すこと。出さないと、ガターに落ちた投球を
    「物理が変わった」と誤読する（実際に一度誤読した）
  - **立ち位置は `_sideOffset` を直接指定する必要がある。**
    投げたあとに `rb.position` へ書いても PhysX には届かない。
    リフレクションで `_sideOffset` を入れ、`ApplySpawnPosition()` を呼ぶこと
  - **速い掃引でピンだけ戻すときは `ThrowSequencer.StopLane()` を呼ぶこと。**
    呼ばないと投球係が投球数を数え続け、**2投ごとにレーンが進む**。
    気づかないまま別のレーンを測ることになる（6本目の掃引で実際にやった）。
    見抜く手がかりは**速度 8.36 m/s**。平らなレーンの回帰基準値なので、
    ギミックのあるレーンでこの値が出たら別のレーンを測っている
  - **★レーンを読み込んだ直後の1投目は外れることがある。**
    捨て投げを1回してから測ること。1投目だけ違う値が出て、
    「変わった」と誤読する
  - **★`Destroy()` はフレームの終わりに効く。**
    同じフレーム内で `StartGame` を連打すると古いレーンがまだ生きている。
    レーンごとの灯や部品の数を数えるときは、**フレームを跨いで**数えること
    （数えないと 5 → 10 → 15 と増えて見え、「漏れている」と誤読する）。
    ただし古いレーンの部品は**消す指示の時点で止まり、`OnDisable` の戻す処理も済んでいる**
    （8→9本目を同じ呼び出しの中で切り替えて実測：古い `LaneLighting` は無効、照明は9本目の値）。
    なので「新しいレーンが古いレーンの設定を元の値として覚える」ことは起きない
  - **★直したら「1〜10本目の通し確認」をする（回帰の基準）。**
    レーンを単独で読み込む計測では、前のレーンの後始末と重なる不具合が見つからない
    （9本目の「3本しか倒れない」は単独だと必ず10本だった）。
    投球係に任せて1→10本目を2ゲーム以上通し、10本目で終わること・タイムアウト0・本数の矛盾0・
    警告とエラー0・各レーン開始時のピンと共有設定が2ゲームで同じことを見る。
    **8本目は速8・真ん中（ピンが場外に止まる）→ 9本目は神殿に入る投球**を必ず含める。
    手順と見る項目の詳細は `仕様.md` の「物理の回帰チェック基準」
  - **★通しでは「動いているべきものが動いているか」も数値で見る。**
    各投球の構え中に0.6秒あけて2回測り、0.5cm か 0.5°以上動いていること。
    4本目の動く壁・5本目の円盤・7本目の羽根・8本目の回るスポット・
    9本目の神殿（1投目・2投目とも）・10本目の輪1・輪2・床B（1投目・2投目とも）。
    本数とエラーしか見ていなかったので、9本目で2投目の構え中に神殿が止まっていたのに気づけなかった。
    **9本目の2投目を必ず含めること**（1投目で阻まれる投球を入れる）
  - **Play 中にプレハブを書き換えると反映されないことがある。**
    書き換えたら Play を入り直すこと。
    「直したはずなのに結果が変わらない」はこれを疑う
  - **「初接触z」は低い弾道では当てにならない。**
    ボールが隙間の上を通過している最中に高さ 0.17m を下回ると、そこで記録してしまう。
    隙間の終わりより手前の値が出たら疑うこと
  - **立ち位置の指定が物理に届くまで1フレーム要る。**
    置く呼び出しと投げる呼び出しを**分ける**こと。
    同じ eval の中で続けて投げると指定が効かず、中央から投げたのと同じ結果になる
    （今回、0.30 と 0.45 を指定したのに3本とも中央と同じ値が出た）
    **★同じフレームで投げると、その時点のボールの位置から投げられる。構え中のボールはマウスに付いて動くので、
    マウスの位置しだいで結果が変わる**（9本目で、右端 +0.40 から投げられて柱に当たった）。
    置いた直後に `body.position = ball.transform.position; Physics.SyncTransforms();` で物理側も揃えること。
    通し・9本目の確認スクリプトはこの形に直してある

- **★ボールの衝突検出を `ContinuousSpeculative` にしてはいけない。**
  触れていないピンにも衝突を伝え、連鎖爆発の起点になって10本吹き飛ぶ（9本目で実際に起きた）。
  kinematic の柱へのめり込み（1ステップ分）はこれでは直さない。すり抜けない太さにしてある
- **★スクリプトで新しく作った Lit のマテリアルは、発光を入れても光らない。**
  作った直後は「発光なし（EmissiveIsBlack）」扱いで、URP が `_EMISSION` を切る。
  `globalIlluminationFlags` を 9本目の `TempleNeon` と同じ `BakedEmissive` にすること（10本目で手すりが暗い灰色になった）
- **★`NeonFlow` は子の見た目をすべて集めて色を上書きする。** ネオン以外（ガラス・磁石）と同じ入れ物に置くと、
  それも光ってしまう。10本目は `onlyMaterial` にネオンのマテリアルを入れて絞った（空なら今までどおり全部）
- **9本目の「入る」投球が、まれに柱に阻まれていた件。** 段階6で、確認スクリプトの投げ方が原因の可能性が高いと分かった
  （立ち位置を置いた同じフレームで投げていて、マウスの位置から投げられていた）。直してからは12投中12投、入れば10本
- **★`Lane_Straight` は2・4・5本目の土台（Prefab Variant）。** 直接飾りを足すと3本に出る。
  1本目の飾りは専用の Variant `Lane_OldAlley` に置いた。2本目はもとから専用の Variant `Lane_Tilted` があったので、そこに置いた。
  4・5本目も、使っているプレハブが `Lane_Straight` そのものか専用の Variant かを先に確かめること
- **★傾いたレーンの飾りは `Tilt` の子に置く**（2本目）。物理の床と同じ角度・向きで必ず傾く。海や空など水平のままにしたいものは外に置く
- **★夜の場面の Lit マテリアルは、環境の映り込みを切る**（`_ENVIRONMENTREFLECTIONS_OFF`）。
  切らないとシーンの昼の空が映って、海や甲板が白っぽい灰色になる（2本目）。キーワードを変えた直後は
  シェーダーのコンパイル中だけ画面が水色一色になるので、撮り直す
- **10本目の引き幅 0.75 は、揺れの位相しだいで6回中3〜6回しか回り切らない**（位相はレーンに入るたびに乱数）。
  1回の計測で落ちるものがあっても回帰とはみなさない（`仕様.md` の「ほかのレーンの基準値」）
- **★Volume の設定に部品を足したら、`AssetDatabase.AddObjectToAsset` でファイルに入れること。**
  `VolumeProfile.Add<T>()` だけではメモリ上にしか無く、エディタを起動し直すと消える（8〜10本目の Bloom が
  最初のコミットからずっと効いていなかった。段階6で作り直した）。作ったら、メモリ上の設定を捨てて読み直して確かめる
- **★Bloom は `capture_game_view` に写らない。** 光り方を見るときは `ScreenCapture.CaptureScreenshot` で撮り、
  「ほぼ白の画素の割合」と平均の明るさで比べる（8〜10本目の Bloom の値は、元の値では画面全体が白いもやになったので弱めた）
- **フォントに文字を足したら、アトラスが2枚になった。** 1枚目（1024×1024）は217字で縦が埋まっていたため。
  焼き直しはスクリプトでできる（`焼き込んだ文字.md` の「スクリプトで焼き直す」）
- **`capture_game_view` は画面に重ねた UI を写さない。** 説明文などの UI を撮るときは
  `ScreenCapture.CaptureScreenshot("Assets/Temp/…png")` を使う（撮ったら Assets/Temp ごと消す）
- **形をまとめたファイル（.asset）は、1つ目がファイル名に改名される。** 空の目印を本体にして、形は子として入れる。
  作り直すと参照が切れるので、そのファイルを使う組み立ては全部やり直すこと
- **★kinematic の物は、爆発の起点の判定で「ボール」扱いになる。**
  判定は「相手がピンでなければボール」。9本目は `LaneBlastReference` の
  `kinematicIsNotBall` で除外している。**4本目の動く壁にも同じ甘さがあるかもしれない（未確認）**
- **★前のレーンで場外に止まったピンは、kinematic のまま次のレーンに入ってくる。**
  普通に戻るのは投球係が並べ直すとき。**レーンの開始時にピンやボールの状態を覚えて、
  あとで「元に戻す」作りにしてはいけない**（前のレーンの後始末前の状態を覚えてしまう）。
  9本目はこれで「中まで通しても3本」になっていた。今は降ろすときに必ず kinematic を外す
- **爆発で真上に飛んだピンは、場外として空中で止まり「立っている」と数えられることがある。**
  倒れ判定は水平のずれしか見ないため。9本目は、神殿の爆発で飛ばしたピンを連鎖させないことで避けた
- **画面を撮るときの保存先。** `capture_game_view` はプロジェクトの外に保存できず、
  `Temp/…` と書いても `Assets/Temp/…` に保存される。確認したら
  `AssetDatabase.DeleteAsset("Assets/Temp")` で消すこと
- **計測用の PowerShell の台本は BOM 付き UTF-8 で保存すること。**
  BOM が無いと Windows PowerShell 5.1 が日本語を文字化けして読み、
  「9本目が出たか」「結果の記録が出たか」の文字比較がすべて外れて空振りする
- **Console の末尾を数えて結果を待つ方式は使わない。** 1投ごとに記録が何行も出るので、
  古い結果が末尾から押し出されて件数が増えず、待ちが時間切れまで続く。
  記録の通し番号（seq）が「始めたときより後か」で見ること

- **`BallController.FixedUpdate` は先頭で `_sideSpin` がほぼ0なら即 return している。**
  カーブ0のストレート投球では横回転が0なので、レーン側の力（回る円盤など）を
  この return より後ろに書くと、**ストレートでだけ効かない**。
  「たまに効かない」ように見えて原因が分かりにくい。
  円盤の処理は `_sideSpin` の判定より前に置くこと（`Assets/Scripts/Ball/BallController.cs:416`）

---

## 次にやること（優先順）

1. **段階6：2〜7本目の見た目。** 題材と共通の決まりは `仕様.md` の「段階6：題材と全レーン共通の決まり」
   - 点滅は控えめに（`NeonFlicker` の前例：数秒に一度・0.07秒・4割まで）。画面全体を明滅させない
   - 共有物（ボール・ピン・照明・カメラ・爆発の基準点）は
     `LanePinLook` / `LaneLighting` / `LanePostFx` / `LaneBlastReference` / `LaneFollowView` で
     **そのレーンにいるあいだだけ**変える。戻ることは毎回実測すること
2. 段階6の仕上げ（タイトル・音・ハイスコア・Webビルド）
   - 4本目の動く壁が爆発を誤発火させていないかの確認も段階6に入っている
3. 直すたびに「1〜10本目の通し確認」をすること（`仕様.md` の「物理の回帰チェック基準」）
   - 8〜10本目の Bloom は作り直し済み。2〜7本目の見た目は、Bloom が効いた8〜10本目と比べる
   - 派手さは前半で段階的に上げる：1本目 Bloom なし → 2本目 Bloom 弱め（しきい値 1.4・強さ 0.35）→ 3本目以降はそれ以上

> **9本目の経緯は長いので、作り直す前に `仕様.md` の9本目の節の
> 「採らなかった形と理由」を読むこと。** 同じ試行を繰り返さないため。
> 挙動の精密な調整は打ち切り、見た目に力を回すと決めてある。

---

## 保留にしてあること（今は触らない）

- 起伏レーンでボールが約1.8cm浮く（許容と判断。床の分割を細かくすると悪化する）
- 動く人型が当たり判定より背が高い。32投調べてピンが重なった回数は0だったので何もしない。
  気になったら、頭のぶんの箱を足して `Physics.IgnoreCollision` でボールだけ除外する
- 予測線がピン位置で1割ほど大きく出る（実害なし）
- リザルトの内訳が桁で揃わない（プロポーショナルフォントのため）
- スマホ実機での確認


